document.getElementById("AddtoCart").addEventListener("submit", function (event) {
    // 1. Prevent the page from refreshing:
    event.preventDefault();

    // Make The Cart Data Viewable:
    const
});